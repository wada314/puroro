syntax = "proto3";
message Msg {
    int64 ival = 1;
    repeated int64 rival = 2;
    string sval = 3;
    repeated string rsval = 4;
    
    message SubMsg {
        SubMsg recursive_msg = 1;
        repeated SubMsg recursive_rmsg = 2;
    }
    SubMsg msg = 5;
    repeated SubMsg rmsg = 6;
}

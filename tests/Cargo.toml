[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]


[build-dependencies]
anyhow = "1.0.85"
itertools = "0.12.1"
protoc-plugin-by-closure = { path = "../protoc-plugin-by-closure" }
puroro-codegen = { path = "../codegen" }

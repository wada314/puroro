[package]
name = "tests"
version = "0.13.0"
authors = ["Shohei Wada <pc@wada314.jp>"]
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tests-pb = { path = "../tests-pb" }
puroro-inline = { path = "../inline" }
puroro = { path = "../puroro" }
itertools = "0.10"

[features]
default = []

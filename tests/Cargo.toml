[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dependencies]


[build-dependencies]
anyhow = "1.0.95"
itertools = "0.14.0"
protoc-plugin-by-closure = { path = "../protoc-plugin-by-closure" }
puroro-codegen = { path = "../codegen" }

[dev-dependencies]
puroro = { path = "../puroro" }

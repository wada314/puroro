---
description: The `Result` type in the project
globs: 
alwaysApply: false
---
When you see the `Result` type in the project, double check if that's the standard library's `std::result::Result<T, E>` or the project's local overridden definition `type Result<T> =  std::result::Result<T, SomeErrorType>`.
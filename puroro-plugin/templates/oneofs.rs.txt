
{%- for oneof in self.oneofs %}
{%- if !oneof.is_synthetic().unwrap() %}
#[derive(Debug, Clone, PartialEq)]
pub enum {{ oneof.rust_ident() }} {
    {%- for field in oneof.fields().unwrap() %}
    {{ field.rust_oneof_ident() }}(/*TODO*/),
    {%- endfor %}
}
{%- endif %}
{%- endfor %}
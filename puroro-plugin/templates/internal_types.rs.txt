pub trait {{ m.template_field_types_trait }} {
    type ImplTag;
    {%- for f in m.fields %}
    type {{ f.template_type_name }};
    {%- endfor %} {#- for f in m.fields #}
}

impl {{ m.template_field_types_trait }} for ::puroro::internal::OwnedFields {
    type ImplTag = ::puroro::tags::OwnedImpl;
    {%- for f in m.fields %}
    type {{ f.template_type_name }} = {{ f.simple2_field_type }};
    {%- endfor %} {#- for f in m.fields #}
}
